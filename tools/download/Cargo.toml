[package]
name = "download"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
common = { path = "../common" }

wit-bindgen = "0.60.0"
waki = { version = "0.5.1", features = ["json"] }

schemars = "1.2.2"
serde_json = "1.0.151"
serde = { version = "1.0.229", features = ["derive"] }
